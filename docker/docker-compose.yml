services:
  postgres:
    container_name: postgres_invest
    image: postgres:18.3
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
    - ./postgres-data:/var/lib/postgresql/data/18/main
    ports:
      - "5434:5432"